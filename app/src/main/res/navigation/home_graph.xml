<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/home_graph"
    app:startDestination="@id/home">

    <fragment
        android:id="@+id/home"
        android:name="com.listocalixto.android.rembrandt.feature.home.HomeFragment"
        android:label="HomeFragment"
        tools:layout="@layout/fragment_home" />

    <include app:graph="@navigation/display_image_fragment_graph" />

    <action
        android:id="@+id/show_artworkDetail"
        app:destination="@id/display_image_fragment_graph" />

</navigation>
